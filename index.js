/**
 * Cyber Particle Network — node half.
 * The empty apply exists so the row activates in the host loader; the browser
 * half ships the particle-network overlay through exports["./client"].
 */
export function apply() {}
